# FUSE_KVM_creator

# Description
The KVM creation script allows a validator to create a KVM, set it up correctly and start validating all in one press.

Simply run the command

sudo ./FUSE_KVM_creator.sh

wait ~10minuites and away you go. The validator_list.csv file is used to hold a list of current KVMs running on the host machine
